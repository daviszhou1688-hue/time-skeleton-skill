---
name: time-skeleton
description: 搭建「时间骨架」个人习惯打卡与提醒系统。当用户想要建立一套规律作息的自律打卡看板、每日定时提醒（就寝/起床等）推送、微信打卡登记闭环时使用。该 skill 会引导 WorkBuddy 完成：创建资料库数据库（骨架项字段）、生成观测看板页（纯展示、含本周/14天达标率/起床时间趋势）、配置微信 ClawBot 定时提醒任务、以及打卡回复的登记闭环。适用于希望用 Agent 自动搭建一个可复用、可扩展的个人习惯追踪系统的场景。
agent_created: true
---

# Time Skeleton · 时间骨架打卡系统

一套「规律作息 + 每日打卡 + 定时提醒 + 观测看板」的个人自律系统。核心思想：**管行为、不管生理结果**，用"三态打卡（✅达标 / 🟡弹性卡 / ❌未执行）+ 滚动达标率 + 防崩盘机制"来养成稳定的时间骨架（就寝、起床等），而不是用"全有全无"的连续打卡逼自己焦虑。

## 何时使用

用户提出以下任一诉求时，加载本 skill：
- 想搭一个"作息打卡/习惯追踪/自律看板"
- 想要"每天定时提醒我睡觉/起床/做某事"并推送到微信
- 想要"我在微信里回复打卡，Agent 自动登记到看板"
- 想要一套可复用的个人习惯追踪系统模板

## 系统组成（4 个组件）

1. **资料库数据库**：打卡数据唯一数据源（字段 = 骨架项，每个骨架项是 select 三态 + 备注/精力等）
2. **观测看板页**（资料库 page，纯展示）：今日概览、本周打卡完成情况、14 天滚动达标率、14 天打卡矩阵、起床时间趋势
3. **定时提醒任务**：到点自动推送到微信（微信 ClawBot 通道）
4. **打卡登记闭环**：用户微信回复 → Agent 解析 → 写入数据库 → 看板自动刷新

## 搭建流程（按顺序执行）

> 详细步骤、字段定义、看板代码、登记规则见对应 references 文件。搭建时按需读取，不要一次性全加载。

1. **建数据库** → 读 `references/01-搭建步骤.md` 的「第 1 步：建数据库」，用资料库 skill 创建，字段含就寝/起床（默认骨架项）
2. **建看板页** → 用 `assets/kanban-template.html` 作为模板（已含全部渲染逻辑），把其中 `<资料库ID>` 占位符替换成第 1 步拿到的真实 database_id，再发布为资料库 page
3. **配定时提醒** → 读 `references/01-搭建步骤.md` 的「第 3 步：配定时提醒」，用 automation 工具创建起床/就寝两个提醒任务（prompt 里调用微信推送命令）
4. **配微信推送通道** → 读 `references/02-微信推送配置.md`，安装 wechat-clawbot-push MCP 并完成授权，让提醒能推到微信聊天窗口
5. **写打卡登记交接** → 读 `references/03-打卡登记闭环.md`，生成一份给微信 ClawBot 读的交接文档，让它能解析用户打卡回复并写库

## 核心原则（必须遵守）

- **数据源唯一**：资料库数据库是唯一真相，看板只读它，其他都是副本
- **三态口径**：✅达标 / 🟡弹性卡（计划内例外，每周每项限 2 张）/ ❌未执行（必须写原因）
- **管行为不管结果**：失眠、数据差都不算失败，只记 100% 可控的行为
- **看趋势不看链条**：滚动 14 天达标率 ≥80% 为目标，不做"连续打卡 N 天"这种焦虑型指标
- **可扩展**：默认就寝+起床两项，新增项目改看板模板里的 `ITEMS` 数组 + 数据库加字段即可（见 `references/04-扩展项目指南.md`）

## 资源清单

| 文件 | 用途 |
|---|---|
| `references/01-搭建步骤.md` | 分步搭建流程 + 数据库 schema + 定时任务规则 |
| `references/02-微信推送配置.md` | 微信 ClawBot 推送通道配置（wechat-clawbot-push） |
| `references/03-打卡登记闭环.md` | 打卡回复解析 + 写库规则 + 交接文档模板 |
| `references/04-扩展项目指南.md` | 怎么加第 3、4 个项目 |
| `assets/kanban-template.html` | 脱敏后的看板 HTML/JS 模板（含占位符） |
